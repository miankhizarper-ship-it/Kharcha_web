# Netlify configuration — Kharcha landing page (Next.js server mode).
# The Next runtime is auto-installed by Netlify when it detects Next.js.

[build]
  command = "npm run build"

[build.environment]
  NODE_VERSION = "20"
